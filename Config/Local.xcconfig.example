// Copy to Config/Local.xcconfig (gitignored). A stable identity keeps macOS from
// dropping the Screen Recording grant on every rebuild.
// Find your identity hash with: security find-identity -v -p codesigning
CODE_SIGN_IDENTITY = <40-char SHA-1 of your Apple Development certificate>
DEVELOPMENT_TEAM = <your 10-char Team ID>
