// Default signing: ad-hoc ("Sign to Run Locally"), so anyone can build without an
// Apple Developer account. To use your own certificate, copy Local.xcconfig.example
// to Local.xcconfig (gitignored) and set your identity and team there.
CODE_SIGN_STYLE = Manual
CODE_SIGN_IDENTITY = -
DEVELOPMENT_TEAM =

#include? "Local.xcconfig"
